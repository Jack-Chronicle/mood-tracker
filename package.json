{
  "name": "obsidian-mood-energy-plugin",
  "version": "1.0.0",
  "description": "A plugin for Obsidian to quickly insert mood and energy level values.",
  "main": "plugin/main.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --external:obsidian --outfile=plugin/main.js && npm run copy-manifest",
    "copy-manifest": "pwsh ./copy-plugin-files.ps1"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "mood",
    "energy",
    "slider"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "obsidian": "^0.12.0"
  },
  "devDependencies": {
    "typescript": "^4.0.0",
    "esbuild": "^0.20.0"
  }
}